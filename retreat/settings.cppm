module;

#include "tstring.h"

export module settings;

export enum class Setting {
    
};

export class Settings {
public:
    Settings(tstring file);

private:

};

module :private;

Settings::Settings(tstring file) {

}